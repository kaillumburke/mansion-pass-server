[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn sign_pass:app --bind 0.0.0.0:$PORT --workers 2 --timeout 30"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
